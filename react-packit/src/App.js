import { Button, IconButton } from './components/button';
import ProductCard from './components/cards';

function App() {
  return (
    <div>
      {/* <Button label={'simple button'} type={'button-base'} />
      <Button label={'action button'} type={'call-to-action'} />
      <IconButton label={'email'} /> */}
      <ProductCard />
    </div>
  );
}

export default App;
